.expo/
dist/
web-build/

# EAS local build / credentials state
.eas/
credentials.json

# iOS / Android signing material — never commit
*.mobileprovision
*.p12
*.p8
*.keystore
*.jks

# Native projects are generated by `expo prebuild` (Continuous Native
# Generation). app.json and plugins/ are the source of truth; see RELEASE.md.
/ios
/android
